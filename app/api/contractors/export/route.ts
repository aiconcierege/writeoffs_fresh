import{NextResponse}from'next/server';import{createServerSupabase}from'../../../../utils/supabase/server';import{contractorSummaryCsv,listContractorSummaries}from'../../../lib/bookkeeping/contractor-awareness'
export async function GET(request:Request){const supabase=await createServerSupabase(),{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'unauthorized'},{status:401});const{data:business}=await supabase.from('businesses').select('id').eq('owner_user_id',user.id).single();if(!business)return NextResponse.json({error:'business_unavailable'},{status:404});const year=Number(new URL(request.url).searchParams.get('year')??new Date().getFullYear());const rows=await listContractorSummaries({supabase,businessId:business.id,taxYear:year});return new NextResponse(contractorSummaryCsv(rows),{headers:{'content-type':'text/csv; charset=utf-8','content-disposition':`attachment; filename="contractor-summary-${year}.csv"`}})}
