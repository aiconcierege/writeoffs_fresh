import {NextResponse}from'next/server';import{createServerSupabase}from'../../../utils/supabase/server';import{validateInvoice}from'../../lib/invoices/validation'
export async function POST(request:Request){const supabase=await createServerSupabase();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'unauthorized'},{status:401});let body:unknown;try{body=await request.json()}catch{return NextResponse.json({error:'Enter the invoice details.'},{status:400})};const parsed=validateInvoice(body);if(!parsed.ok)return NextResponse.json({error:parsed.error},{status:400});const key=request.headers.get('idempotency-key')??'';if(!/^[a-zA-Z0-9:_-]{1,120}$/.test(key))return NextResponse.json({error:'A safe request identity is required.'},{status:400});const v=parsed.value;const{data,error}=await supabase.rpc('create_canonical_invoice',{p_customer_name:v.customerName,p_customer_email:v.customerEmail,p_amount_cents:v.amountCents,p_currency:v.currency,p_issue_date:v.issueDate,p_due_date:v.dueDate,p_description:v.description,p_job_label:v.jobLabel,p_location:v.location,p_note:v.note,p_request_key:key});if(error)return NextResponse.json({error:'Invoice could not be created.'},{status:400});return NextResponse.json({ok:true,id:data})}
