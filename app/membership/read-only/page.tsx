import Link from 'next/link';import{redirect}from'next/navigation';import{createServerSupabase}from'../../../utils/supabase/server';import{loadCustomerEntitlements}from'../../lib/membership/entitlements'
export default async function ReadOnlyMembershipPage(){const supabase=await createServerSupabase(),{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const membership=await loadCustomerEntitlements(supabase);if(membership.lifecycle!=='expired_read_only')redirect(membership.lifecycle==='none'?'/membership':'/home');return <main className="app-page"><section className="page-container page-container-narrow"><p className="eyebrow">Historical records</p><h1 className="page-title">Your records are still here.</h1><p className="page-description">View and export the records WriteOffs already organized. Restart your membership to add new activity and resume automatic organization.</p><div className="mt-8 flex flex-wrap gap-3"><Link className="btn btn-primary" href="/membership">Restart membership</Link><Link className="btn btn-secondary" href="/transactions">View transactions</Link><Link className="btn btn-secondary" href="/reports">View reports</Link></div></section></main>}
