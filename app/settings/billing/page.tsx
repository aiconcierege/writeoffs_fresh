import Link from'next/link';import{redirect}from'next/navigation';import{createServerSupabase}from'../../../utils/supabase/server';import{loadCustomerEntitlements}from'../../lib/membership/entitlements';import{membershipPlans}from'../../lib/membership/plans';import{BillingActions}from'./BillingActions'
export const dynamic='force-dynamic';const date=(value:string|null)=>value?new Intl.DateTimeFormat('en-US',{dateStyle:'long'}).format(new Date(value)):null
export default async function BillingPage(){const supabase=await createServerSupabase(),{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const membership=await loadCustomerEntitlements(supabase)
 if(membership.lifecycle==='none')redirect('/membership');if(membership.lifecycle==='expired_read_only')return <main className="app-page"><section className="page-container page-container-narrow"><p className="eyebrow">Membership</p><h1 className="page-title">Historical records access</h1><p className="page-description">Your records are still here. Restart your membership to resume automatic organization.</p><Link href="/membership" className="btn btn-primary mt-7 inline-flex">Restart membership</Link></section></main>
 const plan=membershipPlans[membership.plan!];return <main className="app-page"><section className="page-container page-container-narrow"><p className="eyebrow">Membership</p><h1 className="page-title">{plan.name}</h1><p className="page-description">{plan.descriptor}</p><dl className="mt-8 divide-y divide-slate-200 border-y border-slate-200"><div className="flex justify-between gap-6 py-4"><dt>Status</dt><dd className="font-medium capitalize">{membership.lifecycle.replace('_',' ')}</dd></div>{membership.accessThrough&&<div className="flex justify-between gap-6 py-4"><dt>{membership.cancelAtPeriodEnd?'Access through':'Current period ends'}</dt><dd className="font-medium text-right">{date(membership.accessThrough)}</dd></div>}{membership.scheduledPlan&&<div className="flex justify-between gap-6 py-4"><dt>Scheduled change</dt><dd className="font-medium text-right">Expenses on {date(membership.scheduledEffectiveAt)}</dd></div>}</dl>{membership.lifecycle==='payment_issue'&&<p className="inline-notice mt-6">We couldn’t renew your membership. Update your payment method to keep WriteOffs working in the background.</p>}<BillingActions plan={membership.plan!} authority={membership.authority!} canceling={membership.cancelAtPeriodEnd} scheduledPlan={membership.scheduledPlan}/></section></main>}
