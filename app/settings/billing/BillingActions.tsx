'use client'
import{useState}from'react'
type Props={plan:'expenses'|'business';authority:'stripe'|'grant';canceling:boolean;scheduledPlan:'expenses'|'business'|null}
export function BillingActions({authority,canceling,scheduledPlan}:Props){const[working,setWorking]=useState(''),[message,setMessage]=useState('')
 async function call(url:string,body?:object){setWorking(url);setMessage('');try{const response=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body??{})}),data=await response.json();if(!response.ok)throw new Error(data.error);if(data.url)window.location.assign(data.url);else{setMessage(data.message??'Membership updated.');location.reload()}}catch(cause){setMessage(cause instanceof Error?cause.message:'We couldn’t update your membership.')}finally{setWorking('')}}
 if(authority==='grant')return <p className="mt-6 text-sm text-slate-700">This Business has explicit prelaunch access. Stripe billing controls are not attached.</p>
 return <div className="mt-7 flex flex-wrap gap-3">{scheduledPlan&&<button className="btn btn-secondary" disabled={!!working} onClick={()=>call('/api/membership/change',{action:'cancel_downgrade',requestKey:crypto.randomUUID()})}>Keep my current membership</button>}<button className="btn btn-secondary" disabled={!!working} onClick={()=>call('/api/membership/portal')}>Manage payment method</button>{canceling?<button className="btn btn-secondary" disabled={!!working} onClick={()=>call('/api/membership/change',{action:'resume',requestKey:crypto.randomUUID()})}>Keep membership</button>:<><button className="btn btn-danger" disabled={!!working} onClick={()=>call('/api/membership/change',{action:'cancel',requestKey:crypto.randomUUID()})}>Cancel membership</button><p className="w-full text-sm leading-6 text-slate-600">Canceling stops renewal. Your paid service continues through the current period with no partial-month refund. After that, your records stay available to view and download for 12 months before scheduled deletion.</p></>}{message&&<p className="w-full text-sm text-slate-700" role="status">{message}</p>}</div>}
