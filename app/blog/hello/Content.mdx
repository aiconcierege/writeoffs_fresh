# Hello MDX 🚀

This is Markdown rendered inside a TSX page.

<MyButton>Click Me</MyButton>

export function MyButton({ children }) {
  return (
    <button style={{ padding: '8px 12px', background: '#10B981', color: 'white', borderRadius: '6px' }}>
      {children}
    </button>
  );
}
