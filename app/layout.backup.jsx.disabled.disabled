// app/layout.jsx
import "./globals.css";

export const metadata = {
  title: "WriteOffs.io",
  description: "Deduction-first app for 1099 life.",
  themeColor: "#ffffff",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className="h-full">
      <body className="h-full antialiased">{children}</body>
    </html>
  );
}
