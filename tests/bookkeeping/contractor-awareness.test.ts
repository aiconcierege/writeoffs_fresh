import{describe,expect,it}from'vitest';import{contractorSummaryCsv,evaluateContractorAwareness}from'../../app/lib/bookkeeping/contractor-awareness'
describe('contractor information-reporting awareness',()=>{it('fails closed instead of declaring a filing obligation from dollars alone',()=>{expect(evaluateContractorAwareness({totalPaidCents:100000,paymentMethods:['unknown'],w9Status:'on_file',attentionAmountCents:60000})).toBe('information_incomplete');expect(evaluateContractorAwareness({totalPaidCents:100000,paymentMethods:['check'],w9Status:'unknown',attentionAmountCents:60000})).toBe('w9_needed');expect(evaluateContractorAwareness({totalPaidCents:100000,paymentMethods:['check'],w9Status:'on_file',attentionAmountCents:60000})).toBe('potential_1099_attention');expect(evaluateContractorAwareness({totalPaidCents:100000,paymentMethods:['check'],w9Status:'on_file',attentionAmountCents:null})).toBe('information_incomplete')});it('tracks low totals and exposes no-current-action without asserting filing',()=>{expect(evaluateContractorAwareness({totalPaidCents:50000,paymentMethods:['check'],w9Status:'on_file',attentionAmountCents:60000})).toBe('tracking');expect(evaluateContractorAwareness({totalPaidCents:0,paymentMethods:[],w9Status:'unknown',attentionAmountCents:null})).toBe('no_current_action')});it('exports factual, versioned tax-time context',()=>{const csv=contractorSummaryCsv([{id:'c',currentEventId:'e',displayName:'Joe Smith',businessName:null,active:true,totalPaidCents:485000,paymentCount:3,paymentMethods:['ach_zelle','check'],w9Status:'on_file',w9EventId:'w',awareness:'potential_1099_attention',taxYear:2026}]);expect(csv).toContain('contractor-awareness:v1');expect(csv).toContain('4850.00');expect(csv).not.toMatch(/must file|required to file/i)})})
