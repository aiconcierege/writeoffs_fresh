import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest'
const sql=readFileSync('supabase/migrations/20260825000400_add_canonical_memberships.sql','utf8').toLowerCase()
describe('canonical membership schema',()=>{it('is Business-owned and isolates provider truth',()=>{expect(sql).toContain('create table public.business_memberships');expect(sql).toContain('business_id uuid not null unique');expect(sql).toContain('create table public.membership_provider_links');expect(sql).toContain('provider_customer_id text not null unique');expect(sql).toContain('security_invoker=true')})
 it('preserves events, grants, and replay-safe provider application',()=>{expect(sql).toContain('create table public.membership_events');expect(sql).toContain('membership history is append-only');expect(sql).toContain('create table public.membership_grants');expect(sql).toContain('stripe_membership_webhook_events');expect(sql).toContain("return 'duplicate'");expect(sql).toContain("return 'stale'")})
 it('prevents customer-authored provider and membership mutations',()=>{expect(sql).toContain('revoke all on public.business_memberships');expect(sql).toContain('grant select on public.business_memberships');expect(sql).toContain('to service_role')})})
